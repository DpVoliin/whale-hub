# 安全策略

鲸鲸（whale-hub）是一个**完全自托管**的个人数据中枢：手机采集器把数据推给你自己的服务器，中枢存进你自己的 SQLite，说话层把脱敏后的事实交给你自己配置的模型。**项目没有官方服务器，作者也接触不到你的任何数据。**

也正因为如此，安全边界掌握在**部署者**手里。本文说明威胁模型、项目已做的防护、以及漏洞怎么报。

---

## 威胁模型

我们按"攻击者能做什么"划分，而不是按"我们怕什么"：

| 威胁 | 场景 | 项目现有防护 |
|---|---|---|
| **T1 · 内网窃听** | 中枢暴露在公网/校园网，报文被中间人读取 | 支持 HTTPS + 自签 CA（`CA` 配置项）；采集器校验服务端证书；中继隧道强制校验证书 |
| **T2 · 未授权写入** | 别人拿到中枢地址往你库里灌垃圾数据 | 所有写入端点要求 `X-Token`；无 token → `401`；MCU 走**独立 token**（固件被抄也不泄露主 token，`hub.json` 里 `mcu.token` 独立生成） |
| **T3 · 配置文件泄露** | `hub.json` 被看到（误提交/误分享） | `.gitignore` 默认排除；README 与部署文档反复提示；每个 token 可独立轮换 |
| **T4 · 隐私外泄到模型服务** | 说话的 LLM 看到原始数据 | **模型输入前脱敏**（`40_privacy.py`）：PII 样式 + 真实形态双重过滤；`/llm-preview` 可查看"到底发了什么"；原文默认不落库 |
| **T5 · 备份被拿走** | 备份文件泄露完整生活数据 | 备份支持加密；保留策略限制历史深度 |
| **T6 · 采集器被伪装** | 别人用 debug keystore 签一个假 APK 覆盖安装 | ⚠️ **当前为 debug 签名，这是已知弱点，见下** |

## 项目已做的防护（可自行核实）

- **鉴权**：`/ingest`、`/metrics`、`/decision` 等端点全部校验 token（实测无 token → 401）
- **脱敏**：6 条 PII 规则 + 29 条真实形态样本的回归测试（`tests/test_privacy_regression.py`）在 CI 里跑
- **独立 MCU token**：单片机走自己的 token，且带序号 + 校验和
- **防火墙收口**：部署脚本默认只开放必要端口
- **加密备份 + 日志轮转**：每日 04:00 备份滚存 7 份，日志超 50MB 归档
- **零第三方依赖**：运行时仅 Python 标准库 → **不存在"依赖被投毒"这条攻击面**（这是零依赖被低估的安全价值）

## 已知弱点（诚实列出，不藏）

1. **Android 采集器使用 debug 签名**（`collector/app/build.gradle.kts`）——debug keystore 口令是公开的 `android`，任何人可以签一个同签名的 APK 覆盖安装。**目前仅影响自用场景**；若要分发给别人，必须先换 release 签名（已在路线图中）。
2. **无速率限制**：拿到 token 后可高频写入。自用场景影响有限。
3. **`/erase` 等破坏性端点若加入，需二次确认**（尚未实现）。

---

## 报告漏洞

**请勿开公开 issue。** 用以下任一方式私下报告：

- **邮箱**：`security@<你的域名>`（部署时替换为真实地址）
- **GitHub 私密漏洞报告**：仓库 → Security → "Report a vulnerability"（Security Advisory 功能）

请附：影响版本、复现步骤、影响判断。**不需要**提供武器化利用代码。

## 响应时间线（承诺）

| 阶段 | 时限 |
|---|---|
| 确认收到 | **72 小时内** |
| 初步评估与分级 | **7 天内** |
| 修复或缓解方案 | **30 天内**（严重问题优先） |
| 公开披露 | 修复发布后，与报告者协商 |

这是个**个人维护的项目**，没有安全团队。上述时限是我能真实做到的承诺；若某次无法按时，我会在 7 天节点主动说明进展。

## 支持范围

- ✅ 最新版本 + 上一个版本
- ✅ 中枢（`hub/`）、采集器（`collector/`）、说话层（`speaker/`）
- ❌ 你自己改过的分支、第三方扩展（`hub/ext/`）
- ❌ 你自己配置的 LLM 服务商的安全问题（那是它们的问题）

## 致部署者

这个项目的安全**一半在你的部署方式**：

- 不要把中枢直接暴露在公网裸跑；用 WireGuard/反代 + HTTPS
- `hub.json` 不要提交、不要发截图、不要贴进聊天
- 定期检查 `hubctl` 的状态输出里有没有异常来源
- 换设备时记得**撤销旧 token**
