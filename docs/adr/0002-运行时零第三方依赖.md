# ADR 0002：运行时零第三方依赖，开发依赖单独放

**背景**：想加测试、lint、脱敏回归，这些工具生态天然是第三方。

**决定**：中枢与说话层**运行时**只用标准库；`pytest/hypothesis/ruff` 等只作**开发依赖**（CI 与本地跑）。

**为什么**：① "clone 即跑"的前提是没有依赖要装 ② 少一类"半年后依赖烂掉"的维护面
③ 需要第三方库的场景走 `hub/ext/` 外挂，由使用者自己装、自己承担许可责任。

**边界（很重要）**：这两件事**不能混**——把测试工具塞进运行时就等于把卖点拆了。
